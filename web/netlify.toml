#[build]
#command = "yarn workspace @kleros/kleros-v2-contracts install && yarn workspace @kleros/kleros-v2-web install && yarn workspace @kleros/kleros-v2-contracts build && yarn workspace @kleros/kleros-v2-web build"

## Yarn 3 cache does not work out of the box as of Jan 2022. Context:
## https://github.com/netlify/build/issues/1535#issuecomment-1021947989
[build.environment]
#NETLIFY_USE_YARN = "true"
NETLIFY_YARN_WORKSPACES = "true"
#YARN_ENABLE_GLOBAL_CACHE = "true"

## Yarn 3 cache does not work out of the box as of Jan 2022. Context:
## https://github.com/netlify/build/issues/1535#issuecomment-1021947989
NODE_VERSION = "16"
#YARN_VERSION = "3.1.1"
#YARN_CACHE_FOLDER = "/opt/buildhome/.yarn_cache"
#NPM_FLAGS = "--version"

#[[plugins]]
#package = "/netlify-yarn-berry-plugin"