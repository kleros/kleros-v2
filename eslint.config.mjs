//Note that workspaces should have their own lint config. This is a fallback.
//Any eventual files not caught by workspace configs, will be caught in root, and the default rules will be applied.
export { default } from "@kleros/kleros-v2-eslint-config/flat.mjs";
